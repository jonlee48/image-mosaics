title: Image Mosaics
url: https://jonlee48.github.io/image-mosaics
favicon: "./logo.png" # relative path to site's favicon

permalink: /:slug

# Build settings
remote-theme: moonwalk

theme_config:
  appearance: "dark" # can be "light", "dark" or "auto"
  appearance_toggle: true # if appearance can be switched by user
  date_format: "%Y-%m-%d" # customize how date is formatted
jekyll: true

highlighter: rouge

sass:
  style: :compressed

plugins:
  #- jekyll-feed
  #- jekyll-seo-tag

exclude:
  